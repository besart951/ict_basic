# Dein erster Commit

## Schritt-für-Schritt

```bash
git init
git add .
git commit -m "Erster Commit"
```

## Push zu GitHub

```bash
git remote add origin <DEIN_REPO_LINK>
git branch -M main
git push -u origin main
```

## Pull von GitHub

```bash
git pull origin main
```
